package Managefolio.admin.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class StartupUserLoader {
    // Removed default user creation - use only database authentication
    // Create users manually through the application or SQL scripts
}
